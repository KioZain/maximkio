/* ==========================================================================
 * homeCanvas — заготовка под холст в баннере на главной.
 *
 * Сейчас пусто намеренно. Всё подключение уже на месте, поэтому эффект
 * можно добавить, не трогая сборку:
 *
 *   - чанк собирается из этого файла (config/webpack.common.js) и
 *     подключён только к главной (config/i18n/config.js, страница index);
 *   - в разметке есть контейнер (src/index.html):
 *
 *         <div class="A_CanvasBeautiful" id="home-canvas">…</div>
 *
 *     его вид (скругление, фон, max-height) — в A_CanvasBeautiful.css.
 *
 * Как добавлять: здесь — только поиск контейнера и запуск, сама логика
 * (canvas, WebGL, SVG-анимация) — в utils/, по примеру marquee.js и
 * utils/horizontalLoop.js. Холст вставлять в начало контейнера
 * (host.prepend), чтобы время и город из разметки остались поверх.
 * Для движения — учитывать prefers-reduced-motion и останавливать цикл,
 * когда баннер не виден, как в reveal.js и magnetic.js.
 * ========================================================================== */
